workspaces:
  docker-worker:
    type: worker
    agent: opus
    isolation: docker
    docker_image: clc-worker:latest
coordinators:
  dev:
    workspace: docker-worker
    max_workers: 1
    selector:
      label: backend
      project: v0.1.0
supervisor:
  poll_interval: 5
