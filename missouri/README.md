# missouri

Show-me-state: e2e testing as directed graphs of filesystem states.

Define states as directories, transitions as shell commands, and assertions as shell expressions. Missouri walks every path through the graph, running transitions and verifying assertions at each state.
